---
/**
 * AuthorBio Component
 * Displays author information
 */

interface Props {
  author?: string;
  bio?: string;
  avatar?: string;
}

const {
  author = 'Indoor Climbing Gym Team',
  bio = author === 'Indoor Climbing Gym Team'
    ? 'The Indoor Climbing Gym team is dedicated to helping climbers find the perfect gym and improve their climbing skills. We share expert advice, gym reviews, and climbing tips from our community of passionate climbers.'
    : `${author} is a passionate climber and contributor to IndoorClimbingGym.com, sharing insights and experiences from the climbing community.`,
  avatar = '/logo.svg',
} = Astro.props;
---

<div class="bg-[#1f1f1f] rounded-xl p-6 my-8 border border-white/10">
  <div class="flex items-start gap-4">
    <img
      src={avatar}
      alt={author}
      class="w-16 h-16 rounded-full object-cover flex-shrink-0 border-2 border-[#2979ff]/30"
      loading="lazy"
    />
    <div>
      <h3 class="font-bold text-lg text-white mb-1 font-display">About {author}</h3>
      <p class="text-white/70 text-sm leading-relaxed">{bio}</p>
    </div>
  </div>
</div>
